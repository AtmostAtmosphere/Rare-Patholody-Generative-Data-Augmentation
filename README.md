# Rare-Patholody-Generative-Data-Augmentation
We propose a generative data augmentation framework utilizing a Denoising Diffusion Probabilistic Model (DDPM) to synthesize high-fidelity brain tumor MRI scans. By integrating these synthetic images into the training pipeline of a ResNet18 classifier, we improve the accuracy of classifier in contrast to training based only on raw images.
